# v0.2.0 看清再行动 · closure charter

Pre-change HEAD: 943f3cca. Working tree clean. Canonical source identity before changes: 788ec52f2f0488f5d77faca9aab9ea0617e3c22edcde6f868430ddc887b7a3bb.

Child-experience question: can the player identify a destination before acting, select a scene object, and express one clear intention without navigating persistent mode menus? Preserve every puzzle decision, ordinary legal route, capacity/ownership/contact/visibility constraint, anonymous v1 save and three-room boundary.

Findings: persistent mode menus expose irrelevant actions; identical navigation dots lack a world/name correspondence; pointer-up forces ring mode for objects and player movement for release/NPC destinations; dynamic do-index identities lose semantic focus; separate walk/action steps inflate activation count; animation UI lacks a cancellable multi-step intention; camera preview obscures the scene. All are authorized repairs.

Allowed closure: games/oddity-puzzles runtime, scene, styles, local intention/landmark data, necessary model diagnostics; existing art only receives landmark/switch/slot or camera corrections; tests/oddity-puzzles*, tools/oddity-puzzles, docs/oddity-puzzles, handoffs/oddity-puzzles-v0.2.0*. Minimal routing/deployment changes only if a current reproduced defect requires them. No unrelated game, shared engine, source cleanup, save namespace migration, new room or item.

Baseline: actual mouse/touch/keyboard inputs from home on unchanged v0.1.0, full input traces with starting state. Counts exclude tutorial/setup outside the declared segment; modes and keyboard navigation count separately. No code-derived counts represented as observations.

Acceptance: existing rule and alternative-order tests; new transactional intent/undo/cancellation tests; scene-before-action screenshots and semantic anchor review; actual-input three-room completion and recovery in desktop mouse/keyboard, 390 touch, 768/1024 tablets in both orientations and 200% zoom; labels/routes/hit areas/focus, empty photo return, unique restored door, no unintended actor/tool switches, old save reload, cancel/reset/blur. Production/offline relocation and local/remote/Pages identity checked separately. Final source-bound evidence, one commit/push, scoped Recycle Bin cleanup of task screenshots. Automated evidence does not establish child comprehension, fun or physical-device performance.

Stop only at the complete authorized delivery or a demonstrated external blocker; a partial passing implementation is AUTO_REVISE. No new release or learning claims before final audit.
