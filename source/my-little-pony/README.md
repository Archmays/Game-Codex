# Reference-image archive pointer

The non-runtime reference images formerly stored here were copied to the external content-addressed archive and hash-verified before removal from the Git current tree during `GAME-CODEX-PORTFOLIO-FOUNDATION-01`.

Original paths, byte sizes, SHA-256 identities, and canonical archive object paths are recorded in `reports/archive-manifest.json`. No game import, route, build, or test reads these images.
