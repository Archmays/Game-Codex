# Retired council box

The requested retirement removes the council box from the current lobby, box chooser, dynamic imports, exclusive assets and generators. Existing unknown-scene handling opens Window Breeze for the old scene URL. Window Breeze and Dresden retain their original implementation and assets.

The exact pre-deletion inventory was frozen in the owning private production task. There were no external delivery paths in the old task records. Shared fonts, generic historical sources, common audio, source history and real browser saves are preserved. Removing two exclusive keys from the backup inventory does not delete localStorage records.

Checks: remaining box model tests, Vault and inventory contracts; production build; portfolio and evolution checks; readiness and interaction validators. Desktop mouse and keyboard and 390px emulated touch exercised the old URL fallback, assembly in both boxes, chooser and return. Same-context reads confirmed the first box's save survives switching. Runtime audio had both music/environment loops, 24 decoded buffers and zero pending loads. No page errors or old runtime requests were observed. The 41 protected shared runtime/audio/Dresden files retained their pre-change SHA-256 hashes.

Final local unit suite: 48 files, 600 tests passed. Initial inventory-count failures were corrected in affected validators and tests; no functional assertions were removed. Production assets contain no old council runtime or media.

Machine semantic review: child entry is valid; menu choices match available boxes; no old lesson is linked; no actual save removal or unrelated runtime change. Verdict: PASS_MACHINE for the local retirement scope. This does not claim physical-device testing or child learning outcomes. Deployment is verified separately against the pushed commit.
