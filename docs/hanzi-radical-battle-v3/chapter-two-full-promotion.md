# 第二章全章玩法与视觉收口

任务 `GAME-CODEX-STEP4-HANZI-CHAPTER2-FULL-PROMOTION`。起始本地与远端 main 均为 `ee8d47b36703bb29b75ceff8307a6b841f7377aa`，工作区干净。使用根 AGENTS 的共享／发布车道及 machine-first-game-review、child-first-learning-game、hanzi-structure-quality。原 pilot-six 报告与全局视觉审计为当前参考；自动验证只证明声明的技术和内容合同。

## 实现前审计与允许范围

基线 `pnpm test`：60 文件、483 测试通过。`pnpm build` 通过，保留既有 Phaser 大块提示。已发布报告记载的开发模式传输预算失败是既有诊断，不改阈值；本轮另核生产请求成本。起始源码规范 hash、全文件 SHA256、生产冻结和命令日志保存在 D 盘忽略任务目录。

在隔离 Chromium 1440×900 上，从合成 r1 的 07 起点以实际鼠标输入走完 07–12、三次树心合字和结尾，记录 70 个可见界面状态。旧后半章合字后依次确认完整字、词义、家族说明；连接界面只给真成员，任何两个即过。07“迷”左下包围误排成上／下，魔法与修复均主要用文字卡表达。树心原环境已具中央暗团、三个方向地面线与足够对象空间，直接复用。

儿童体验问题：合字和关系判断能否让孩子看见自己操作的对象、可走的连接和持久修复？学习价值由真实空间合字、固定词境及当前图谱的关系证据分别承担。

允许闭包：第二章 scene definition、engine、pilot-view 的版本分流和对象层；独立 r2 CSS、原图／WebP／来源清单；现有 replay 类型、schema 和新开局版本；相关规则、保存、浏览器、视觉和 affected gate 接线；本报告与视觉审计。前六的定义、r1 reducer 行为和画面保持；旧版本解释不自动迁移。V2、optional、第一／三章内容与规则、270 原始字轮、72／18／36、200 滑轨、目标工坊、英语及37精确存档键保护。修复范围外问题只保留记录。

## 逐段设计（先于产品修改冻结）

| 段 / 原问题 | 固定词境与魔法 | 场景对象与玩家动作 | 规则状态与家族判断 | 恢复 / 验收 |
| --- | --- | --- | --- | --- |
| 07 迷：半包围误排，出口只在文字里 | 迷路／米粒星光辨路；安全出口一直可见 | 用星光照上弯或下弯，两路均能通向可见出口 | 记录所照弯路；对应星点和路径实际改变。沿本段既有青家族连接，所有当前成员均可选，迷为有辶证据的非成员 | 辶左下、米右上。没有假必败路；错误字脉不清照亮状态。静按原图谱仅现代字形连接 |
| 08 思：散叶／思路没有实际变化 | 思考／思路叶图 | 先看可见水流线索或踏石线索，叶片按所选观察顺序组成不同路线 | 记录观察选择与叶片排列；木家族林／松／树全部合法边均接受；思作非成员 | 两种排列与中间连接不同；不要求未见信息，不把田＋心编成字源 |
| 09 语：没有可见声桥 | 语言／语言光桥 | 点亮任一长廊端点，再让另一端回应；不依赖听力 | 两端各有独立回应状态，一端先亮不会伪称全桥接通；草家族全体成员连接实际声纹桥段 | 两种先后顺序都成立，中间亮端不同；错连保留回应。可见回声结在回应后松开 |
| 10 饭：饭香只有文字 | 米饭／饭香补光 | 把暖雾分别送入两组可见小灯，顺序自由 | 两组灯独立由垂暗到挺亮；门家族全体成员接受，关系仅现代字形，不推共同意思 | 饭与食物不是奖励。任一组已亮即保存，另一组仍清楚待恢复；错误可再选 |
| 11 钟：钟声与门影没有因果 | 时钟／钟声定影 | 从可见时钟发出一次波纹，让重叠门影各归其位 | 稳定两扇具体门影，之后囗家族全体合法边控制通路 | 无计时、速度、每日或惩罚；静音保留波纹和门影状态，囗／口不混 |
| 12 钱：齿轮修复只在说明中 | 保留原记录钱包词境与准确字义；场景仅金属光片修锁 | 将两片不带面额的金属齿片分别送入可见缺口，顺序自由 | 两齿口独立填入，之后宀家族安／家连接使锁闩接通 | 不建立货币余额、价格、商店或奖励经济，不使用硬币形奖励。两种中间齿口状态均可刷新恢复 |
| 树心：重复说明屏，没有三个区域根线实际入心 | 保留原情／进／指及青／辶／手的三次组合 | 情用已学心灯舒藤；进沿已接通石路送光；指照清根线。各自合字、施法、合法连边后，把对应区域根线接入中央树心 | 三个区域已修复是前提；三条 root connection 分别保存。第三条接入前不置 component-root-heart 修复；守护者只表现已见根雾、变形影、回声结 | 旧动作与旧解释不变。错误连接或未接通时点击树心不前移；只有全部原故事条件＋三根接入才进入修复／结尾 |

各段家族按现有 episode allocation 取得，目标字不必属于该区域家族。候选为当前 family 的全部成员，非成员从已有 character＋relation 记录取证；接受所有真成员的无向边，完成由入口至终点可达决定。合法尾段连接也接受，但入口未连通时保持未完成。

## 兼容与视觉方案

在产品修改前，由 ee8d47b 原 engine 冻结 legacy 162 个状态前缀，以及 r1 直达／中继两轨 143／153 个前缀。新规则标为 `chapter-two-r2`，新开局与自愿重玩采用 r2；已有 undefined／r1 局保持原规则，不凭猜测升级。priorRuns、已完成／修复／解锁和 checksum 全保留，未来规则／损坏／拒读拒写／跨页换档保护延续。

复用 pilot-six 同一 scene camera、对象定位、热区、操作台、图像复用与响应式规则；新增渲染仅在 r2 的后六段／树心启用。清泉石谷继续用 r1 环境；门影长廊新增一张安静、分层、无文字环境；树心复用 region-ink-king-core。汉字、拼音、按钮、精确声纹／根线和时钟指针程序绘制。对象前后状态共用相机和锚点，状态立即落 reducer，动画不参与保存。

## 验收清单与停止条件

规则：全12段＋树心可达，错误可恢复，多顺序／多路径有不同中间状态；前六新版行为不漂移；旧 oracle 逐前缀相同；r2逐前缀确定性，全部保存负例和跨页字段保留。

浏览器：1440鼠标／390触摸全第二章至第三章入口；768／360关键对象与结构；键盘、静音、减少动态、刷新、返回、魔法书／世界／第一第三章／设置／Vault保存往返。使用真实输入；最终 UI-only 只读画面与可访问名称，执行者为本任务 Codex 时标记交叉自审。

视觉：逐图检查后才接受必要基线；最终代表帧目标≤36，迭代覆盖；冷／热请求、解码、首页0新增请求。运行全 test／build、汉字 complete／content／save／simulation／E2E／visual、portfolio affected 和发布必需门禁。所有结论绑定最终同源树，失败不跳过、不抬阈值。

仅暂存本任务，当前分支正常一次提交推送；核实精确提交的 Pages／Portfolio CI，分别报告 LOCAL／PUSH／PAGES／CI。无整仓ZIP。停止于第二章全章收口。回滚先保全含r2的完整存档，再 git revert 本任务提交，不能假定旧代码能安全写新格式。

## 实现与本轮验证结果

新开局采用 `chapter-two-r2`。07–12 与情／进／指三次树心组合全部使用真实对象状态与可恢复连边；三条区域根线分别接入后，才允许修复 `component-root-heart`。旧 undefined 与 `pilot-six-r1` 的进行中局保持其原解释器，主动重玩才新建 r2，并保留旧局归档。没有自动迁移、清档或强制重玩。

| 检查 | 结果与证据 |
| --- | --- |
| 历史回放 | ee8d47b 的 legacy 162、r1 143＋153，共458个逐动作前缀完全一致；另保留旧4befee7 oracle。新规则有独立的全部前缀、合法多解、错误恢复和接根条件测试 |
| 前六画面 | 1440／390、六遭遇、合字／施法前／连通后，共36组；基线r1、当前r1、当前r2画面SHA完全相同，未更新历史快照 |
| 规则／内容／保存 | 全仓64文件520项通过；汉字complete 18文件159项；20万场景模拟中失败、软锁、不可能状态、回放与续玩不一致均为0。72／18／36、270字轮源、200滑轨与37精确键保持 |
| 浏览器 | 新r2的1440鼠标／390触摸全章到第三章、768／360结构、设置、刷新及跨页检查通过。另有桌面／手机真实Vault导出、预览、恢复，证明单端声纹和legacy／r1归档完整往返 |
| 完整兼容矩阵 | 生产模式覆盖41项实际用例；原套件31项按配置不适用的排除保持，本轮没有新增skip。36次验收运行／19配置通过。历史视觉／ARIA／几何8组测试、83状态通过，未重建基线 |
| UI-only | 本任务Codex进行**交叉自审**，以可见画面、可访问名称、真实触摸和键盘操作从新匿名局走到第三章入口；206次按钮操作，三次树心接入均由顺序Tab（各5次）＋Enter完成。没有读取答案、fixture解法、reducer状态或运行中存档。它不构成独立人员或真人儿童证据 |
| 保护范围 | 对基线中允许修改名单以外的1457个已有文件逐一核SHA256，原始字节一致；没有改其他游戏、第一／三章规则、旧共享资产和内容图谱 |
| 发布门禁 | portfolio一致性／evolution、play-readiness、隐私、热区／滚动、生产烟测、观察工具、数学／中文支持／英语必要门禁均执行；生成英文报告后确认零Git差异。最终同源命令记录、源码SHA、远端精确SHA与结论保存在D盘忽略任务目录 |

生产检查使用现有测试的固定本地来源 `127.0.0.1:5194`，由忽略任务目录中的配置将服务器改为Vite preview。早期5225运行的六个旧slice检查把同机不同端口计为外部来源，已在原固定端口重跑通过；没有修改它们的来源判定。最终生产回归完整重跑，保留原断言。

开发模式的旧传输预算诊断仍开放：本轮实测3,124,059 bytes，大于原阈值3,043,009；此前pilot-six报告已记录同类超限。本轮不抬阈值、不跳过失败，也不把开发服务器传输当生产资源量。生产世界页的同一断言通过，抽样为241,459 bytes／323 ms；具体最终抽样以命令记录为准。本机时序不外推到真实低端设备或弱网。

## 素材、逐图审查与成本

新增1张长廊环境、8个对象原图，全部保留在 `assets/images/hanzi-radical-battle/chapter-two/r2/`；运行WebP仅在 `public/assets/hanzi-radical-battle/chapter-two/r2/`。`PROMPTS.md`记录以【ChatGPT image】开头的实际prompt；`manifest.json`记录工具、用户授权、原图／运行图尺寸、SHA、alpha与编码转换。清泉复用r1环境，树心复用原region-ink-king-core，原V2及r1图像不覆盖。

9张运行图合计371,104 bytes，全部RGBA解码量7,961,760 bytes。8个对象均有真实透明像素，边界没有alpha大于20的截断像素；被淘汰的裁边候选和绘制棋盘底候选未进入public。时钟刻度、指针、锁齿落点及精确连接由程序按最终图像锚点绘制。

本机390 CSS px生产抽样：新增图冷请求375,304 bytes、热请求4,200 bytes（含响应开销），最大等待decode约20.1／16.6 ms；首页新增图请求为0。全部九种图均实际请求和解码，不用仅看文件大小代替页面成本。

逐图审查关闭了手机思路线索句裁切（改成可换行的场景提示）、最终钟面／锁齿落点不匹配、树心说明误沿用水轮场景等问题。最终36张代表帧覆盖完整前后状态、双目标中间状态、360密集字碑、768钟面、三条根线接入及结尾；迭代覆盖写，临时拼图不作为额外交付帧。未以批量接受快照掩盖问题。

## 交付与停止边界

本轮关闭视觉审计V08–V10在第二章r2主线及树心内的剩余项。legacy／r1历史后半章保留原界面语义；其他章节与其他游戏的旧视觉发现仍按原状态保留，没有由这次回归冒充视觉整改。

最终源码、图片清单、命令结果与交叉自审证据冻结后，仅提交本任务差异。LOCAL、PUSH、Pages及Portfolio CI分别核实，Pages与CI必须对应同一个最终SHA；queued／in_progress不算成功。回滚先从Vault保全含r2的完整存档，再 `git revert` 本任务提交；旧代码不得被假定可安全写回r2格式。

任务止于第二章全章收口。真人儿童乐趣、学习效果、真实低端及弱网表现均为 `NOT_CLAIMED`。
