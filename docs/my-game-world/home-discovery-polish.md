# 我的游戏世界：首页与活动发现性收口

任务 `GAME-CODEX-STEP7-HOME-DISCOVERY-POLISH`。起点本地／远端 main 均为 `5527ee2e4b64c13f2953393065b2bf4987a4a0fa`，工作区干净。基线 `pnpm test` 65文件／550测试通过，生产构建通过。旧第二章开发传输预算超限独立保留，不扩大为性能重构。原始证据、逐文件身份和可恢复生产基线仅位于 D 盘忽略目录 `tmp/tasks/GAME-CODEX-STEP7-HOME-DISCOVERY-POLISH/`。

本轮问题：孩子能否看见三个不同世界，并从图像和一句操作说明找到入口；进入英语区域后能否辨别场景操作与拼词活动。学习价值是把入口描述与实际动作、历史记录与实际证据对应；不引入新的教学目标或进度体系。采用共享入口／首页保存／发布车道，使用 `machine-first-game-review`、`child-first-learning-game` 与 Playwright 实际浏览器验证。

## 设计与可证伪验收

| 当前问题（本次冻结生产实看） | 保留项 | 拟改结构 | 可证伪验收 |
| --- | --- | --- | --- |
| V01：390／360首页插画缩成横条，上下大段空场；四张25%窄卡与图像分离；768也有大空场 | 三世界身份、所有现有路由、首页只选择世界 | 桌面三张并列入口；手机三张宽入口纵排；一张入口只有一个包含图文与动作的链接；百宝箱置后 | 1440／390／360／768与真实200%缩放逐图检查；标题／CTA不碎裂；主触控≥48px；无横溢出；允许自然文档滚动 |
| V02：Classic英语卡无图、操作按钮位置与其它两卡失衡 | 当前三张卡、筛选、兼容入口和退役清单 | 三卡同样的图／标题／描述／按钮骨架；英语复用首页图 | 两尺寸实看三卡；标题、媒体高度与按钮视觉重量可比较；未恢复退役入口 |
| V13局部：六个场景入口和其它拼词入口都写“跟着词光走” | 五区域／30任务原顺序、六互动模型和24原流程 | 原区域描述说明可操作场景；六按钮写实际动作，其余写看图拼词 | 六入口分别真实点击；24原入口描述与实际拼词阶段一致；没有新区域／任务 |
| Step6 P3：互动完成的run词册仍写“还没遇见”；原词卡完成统一写“已遇见” | V3所有字段和迁移语义 | 仅消费既有互动完成、原词／句完成；分别显示“场景玩过”“词卡活动完成过”；空状态邀请尝试 | 互动单独／词卡单独／两类同时／无记录／V2迁移状态对照；不声称独立拼写或掌握 |
| Step6 P3：one的帮助说“心形部分”，实际仅o／e红框 | grapheme映射、核心提示和完成条件 | 只给已标irregular-heart的块增加小心形与一句短解释 | one仅o／e有心形，n保持常规；帮助／词册／可选拼词逐图核对 |
| 首页设置重序列化会丢弃扩展，未来／损坏／换档也能被覆盖 | 原精确键、静音／减少动态语义、Vault合同 | 首页局部保留扩展；未知版／损坏／拒读只读；按挂载原字节比较，身份变化永久停止该挂载写回 | 合法旧档／扩展／未来版／坏JSON／拒读拒写／跨页与Vault换档定向复现；原字节保全；37键不变 |

先完成手机与桌面首页样板并自行反审，再扩展 Classic 与英语轻修。优先裁切已正式使用的森林、数学双站、英语码头插画，不用文字UI截图，不为更新而重画。首页只请求入口小图，不预加载子游戏大场景。返回焦点只属于浏览器导航体验，不是新增“继续上次游戏”或保存概念。

## 验证范围与停止条件

允许变更：首页及其设置适配、Classic直接样式／入口图、英语app显示层与直接CSS、必要的首页文档滚动注册及其现有测试／生成消费文档、受影响E2E与经逐图接受的必要基线、本报告和视觉审计。保存模型／键清单、英语内容与互动规则、汉字所有章节、数学两站与12题、滑轨200关／revision、翻牌／拼音、退役集合均保护。每个新增行为有逻辑或真实浏览器检查；不安排真人观察门禁。

必须完成：本次冻结生产UI实看；桌面／触摸手机首页→三世界／Classic→返回与设置；360／768／真实200%；键盘、焦点、触控命中、文档滚动、坏／慢图、前后退／刷新、减少动态、首页保存负例；英语六入口、原流程代表与两类历史状态、heart；pnpm test/build、affected门禁、play-readiness、hit-test／scroll、Save Vault及Pages／CI现有必需门禁。最后在同一源码树上完成UI-only交叉自审、逐图接受必要基线和无更新复验，再一次正常提交／推送；LOCAL／PUSH／PAGES／CI绑定同SHA，pending不计成功。

V15词册缺少局部查找的原问题仍在范围外；本轮只澄清活动状态，不把它记作查找问题关闭。不自动推广其余24任务，不改第三章或增首页功能。回滚先导出并保全本机存档，再正常revert本任务提交。`REAL_CHILD_FUN_AND_LEARNING=NOT_CLAIMED`。

## 实现与资产

首页用三个包含图片、标题、操作说明和动作的原生链接替代分离的场景与四窄入口；整张图片可点击，不再挂载首页Phaser大场景。手机自然滚动，760px以上采用三列；返回入口与滚动位置只存在当前浏览器history条目，不增加保存键。设置关闭恢复按钮焦点，减少动态仅按既有首页语义工作。

Classic保留三产品与筛选，仅补英语封面并统一三卡媒体高度、描述伸展和按钮位置。首页三个720×420运行WebP合计123,702字节：森林34,996、数学70,888、词光岛17,818。来源、授权、裁切流程和原／派生SHA在[资产清单](../../assets/images/home/asset-manifest.json)。全部来自当前正式使用素材，原文件字节未改；没有新生成图、UI截图封面或图片生成prompt。实际首页请求只有这三张入口图，没有子游戏大场景图。

英语仅修改app显示层及直接CSS：保留五区域／30任务顺序；六场景按钮说明选落点、颜色或数量，其余24项写“看图拼词”。场景、原词或句完成分别显示实际活动记录；两者同时存在可并列，无记录使用邀请语。V2迁移成绩也只称词卡活动完成。one的o／e显示♥，n不显示；现有其它irregular-heart映射同样消费原字段，规则与保存结构不变。

首页设置适配保留v1顶层和settings内未知扩展；未来版、坏JSON、非法类型及拒读时不覆盖原字节。写入前比较挂载时读取的原字节，另一页或Vault换档后停止旧挂载写回，拒写也不推进内存设置。未新增键或全局静音联动。

## 最终机器验证

本地详细命令日志、身份和31帧最终证据位于忽略目录 `tmp/tasks/GAME-CODEX-STEP7-HOME-DISCOVERY-POLISH/`。`final/frames.json`记录每帧URL、视口、滚动、控件尺寸、图片身份与截图SHA；`reports/FINAL_VERDICT.json`绑定最终源码、构建、提交和远端运行结果。该目录只含机器／合成验证，不含真实儿童记录。

| 检查 | 已验证结果 |
| --- | --- |
| 单元／内容、类型和生产构建 | 67文件／569测试通过；`pnpm build`通过 |
| 首页与返回 | 1440／390的20项新增检查全通过，随play-readiness烟测共24项通过；360／768另20项首页检查与2项a11y通过 |
| 保存负例与Vault | 首页16项单元检查，合法旧档、扩展、未来、损坏、拒读／拒写／存储访问异常、跨页及真实Vault导入换档覆盖；Vault原5项单元与2项浏览器检查通过 |
| 英语受影响矩阵 | 四配置67项通过；新增发现性、记录与heart共12项通过，30入口顺序与阶段逐项验证，原任务完整流程由现有回归覆盖 |
| 其它消费者 | 数学102项与中文支持30项浏览器检查通过；组合烟测16项覆盖三世界、legacy／r1、拼音、翻牌和滑轨实际交互与返回 |
| 点击与滚动 | 代表点击矩阵14项、滚动矩阵4项通过；36面注册保持，首页改为文档滚动后为33文档／2内部／1锁定 |
| 组合与隐私 | portfolio／evolution、play-readiness、interaction／scroll、中文内容、英语生成报告漂移、natural-use隐私与既有流程检查通过；37精确保存键保持 |
| 视觉基线 | 59张候选中34张有预期变化，经逐图检查后只更新这34张；25张像素一致未改。无更新复验：英语9、首页2、数学6、中文支持3项通过 |

各矩阵已有的视口不适用项保持原条件：英语45、英语视觉3、数学9、数学视觉3等未执行项不计PASS；没有通过新增skip跳过失败，也没有提高阈值。早期失败分别是旧整屏／入口文案断言、可选词标签回归、768反馈层遮挡与CSS静态检查误取限定规则；已按原因修复并复验。静态检查现在读取精确`.wordlight-live`规则，仍要求其不拦截指针。

冻结生产画面完成三套交叉自审视角：儿童入口理解、视觉／可访问性、对抗性返回和保存。执行者为本任务Codex，明确是UI-only交叉自审，不称独立评审。31帧均实际打开检查，三个世界视觉身份明确；390／360首屏有完整世界宽卡及下一张入口，Classic位于末端次级位置；实际操作的run／two仅显示场景玩过，完整cat原流程显示词卡活动完成，只有进入过的jump／one仍是邀请语。

真实Chromium页面缩放API确认200%（devicePixelRatio=2、CSS视口约712×402），没有用CSS缩放冒充。四入口均经键盘Enter进入后Back返回，设置内容可滚动到关闭按钮并恢复焦点；31帧横溢出均为0。三封面全部故障时文字链接仍可进入英语。自动与UI实看不能证明真实儿童乐趣或学习效果。

## 关闭与保留

V01、V02关闭；V13只关闭六任务活动描述的发现性局部问题，五区域地图的对象辨识仍开放；V15查找／分组仍开放，相关历史状态与heart两项P3关闭。受保护的汉字内容／解释器、数学、滑轨、英语content／core／pilot／save、共享保存等231份基线文件字节一致，完整1509份起始清单无文件缺失。

旧开发传输预算失败继续单列：3,124,059字节大于3,043,009字节，未调阈值，也未把它算成本轮新失败或扩展为性能重构。没有真实低端设备／真实弱网／真人学习效果结论。

当前分支正常一次提交、一次推送；发布身份以同SHA的LOCAL／PUSH／PAGES／CI及线上`data-build-commit`回读为准，pending不计成功。最终证据仅保留31帧、必要日志／冻结／验证与清理清单，不制作整仓ZIP或录像库。回滚必须先通过Vault导出本机存档，再revert本任务提交；不改写旧tag或历史。
