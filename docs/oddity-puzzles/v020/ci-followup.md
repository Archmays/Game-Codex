# Pages smoke consumer follow-up

Run [35506486478](https://github.com/Archmays/Game-Codex/actions/runs/35506486478), commit `2c6c17dc0e394b9fe145b9d87d8972f268a99afd`: the production portfolio smoke completed 14 tests and failed only the Oddity desktop/mobile rows. Both waited for the retired `.odd-commands` button `观察窗前` at `tests/e2e/portfolio-smoke.spec.ts:65`.

This is a direct consumer of the authorized interaction change. Repair only that smoke action to activate the current scene landmark. Keep history, undo and public return assertions; additionally verify actual arrival and restored position. A targeted search of other Oddity E2E consumers found no further retired action selector. No workflow, timeout, game runtime, puzzle rule or packaged web byte changes are authorized by this follow-up.

The accepted game source and ZIP remain those recorded in `source-before.json`, `source-after.json` and `package.json` for commit `2c6c17dc`. The follow-up source identity differs only because the canonical algorithm also includes tests. Its local smoke result and unchanged production-input proof are in `ci-followup.json`; final Git/Pages readback remains in the local delivery receipt. This necessary CI repair requires a second ordinary commit/push; no history rewriting.
