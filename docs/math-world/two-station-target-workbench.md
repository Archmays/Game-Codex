# 数学双站地图与目标工坊操作桌

任务 `GAME-CODEX-STEP5-MATH-WORLD-TARGET-WORKBENCH`。起始本地／远端 `main` 均为 `025e48dfd609a30dab10b29204576a77f14ee4ff`，工作区干净。根 AGENTS 的共享／发布路线，使用 `machine-first-game-review`、`child-first-learning-game`。临时证据仅在 D 盘忽略目录 `tmp/tasks/GAME-CODEX-STEP5-MATH-WORLD-TARGET-WORKBENCH/`，不制作整仓 ZIP，不修改模型配置。

## 实现前核查与设计反审

基线源码规范 SHA256：`A21620C95309D8CFEFC999DA545FD1B3208620C952F61CAA1537ADFE0F1D6172`。1498 个跟踪文件的逐文件 SHA256、原12题 manifest 与 Git 身份冻结于 `baseline-identity.json`；生产副本 `baseline-dist/`。基线 `pnpm test` 64 文件／520 测试通过；`pnpm build` 通过，保留既有 Phaser 大块提示。第二章报告中开发模式旧传输预算失败继续单列，不抬阈值或当作本轮新增失败。

隔离 Chromium 桌面1440×900／手机390×844，在家庭 origin `http://127.0.0.1:5175/` 将请求映射到5235冻结生产预览，未访问家庭 profile。`baseline/discovery.json`记录56个真实输入后状态、12张图片，已逐图打开；包括10／12／24首题的选牌、交换、合并、撤销、实时四层提示与完成。映射请求不作为HTTP缓存证据。

| 保留项／原问题 | 本轮变更 | 原因与可证伪检查 |
| --- | --- | --- |
| V03：中央旧钟楼抢眼；手机先整张缩图、后脱节文本卡 | 同一暖色双站构图；建筑与名称／主按钮为同一可操作入口；桌面并列、手机分别取景 | 两站均完整可辨；DOM／焦点slider→target；每站只有一个语义入口；坏图仍有文字按钮 |
| V05：目标、统计、提示、历史和反馈串成多个面板 | 目标数→四张手牌→左右操作位／运算／预览→合并集中在一张桌面 | 390首屏数字清楚、关键动作靠近；提示按需展开，最近一步常见、全部历史可展开 |
| 原表达式树、12题及顺序、所有合法规则 | `model.ts`／`solver.ts`／`puzzles.ts`字节锁定；仅消费原API | 四源牌各用一次，4→3→2→1；同值异来源ID不同；不新增规则或答案路径约束 |
| 牌面值与表达式同等抢眼，选择只有小角标 | 大数字为主，原始／合成来源次级；左／右映射槽与可见标记；保持卡片顺序与新牌焦点 | 原始3和1+2的3独立可选；预览不是新牌；撤销恢复原ID与表达式 |
| 已复现：中途四级提示称“第一步”；完成后提示称“没有通路” | 仍用当前表达式求解，仅修正状态与文案；完成直接说明完成 | 中途称当前下一步；不代操作、不泄露完整解；同值用来源定位 |
| 已复现：4−3，再1+2进入无解；一次撤销后仍无解 | 提示说明逐步撤销寻找可续解状态 | 不保证一次撤销有效，不在每次合法选择后评判标准答案路径 |
| 已复现：1−1得0，再4÷0仅提示不能整除 | 除零／非整除／负数各给明确预览，合并前解释 | 不消费牌或历史；零仍是合法中间值 |
| 已复现：未知字段丢失、损坏字节被写回、挂载后未来档被旧页覆盖 | target独立保存适配，严格读／写保护与挂载身份比对；地图局部身份保护 | 原wins与未知字段保留，重玩幂等；拒读拒写／跨页／Vault换档不能覆盖；不改game-core与37键 |
| 原导航、历史访问、slider宿主及其他产品 | 目标宿主差异仅使用target选择器；地图样式限定math-map | slider截图／定义不漂移；退役URL和lastStation访问语义保留 |

儿童体验问题：能否从建筑直接认出两站，并在一个紧凑工作区看清手中牌、下一次合并与可恢复结果？学习价值仍是数感、运算顺序和完整括号表达式。图像不参与算术规则，不用大背景代替可操作桌面。

允许闭包：`games/math-lab/world/`地图布局与局部保存保护，`games/make-target/`UI／提示文案／独立保存，必要target专用样式迁移与直接测试／入口合同，数学资产与来源清单，本报告及visual audit V03／V05。禁止修改受保护规则三文件、slider200关／宿主构图／revision、汉字legacy/r1/r2及回放／资产、首页／英语／拼音／翻牌、共享game-core、37精确键和存档schema。旧数学运行图仍有Classic消费者，保留。

先做两入口与目标10第一题合并／撤销的真实样板，在1440／390实看后再完善所有状态。最终冻结后交叉自审：不能只是换背景、增加步骤、让图压过数字、破坏ID或越界改规则。常规样式由Codex自审，不新增用户审批。

## 验收与停止条件

原12题分别在1440与390用真实输入完成；补同值不同来源、减法换序、非整除／除零／零、提前目标、无解恢复、完成提示、重玩幂等、换组取消、快速输入／动画取消、焦点／历史。768／360与真实200%缩放检查关键状态。两站路由、返回焦点、刷新／前后退、退役回落、坏图降级；合成37键／Vault往返与存储拒绝。运行受影响逻辑与浏览器检查、test／build及现有生产Pages／CI门禁；最终代表图约24–32帧，逐图审核才更新必要基线并无更新复验。

停止于本任务一次正常提交推送及精确SHA的LOCAL／PUSH／PAGES／CI核查；pending不算通过。回滚只revert本任务，先保全存档。真人儿童乐趣、学习效果与辅助技术认证均不作声明。

## 实现与资产

地图现在每站只有一个原生按钮，图、站名与入口提示共用该按钮；桌面两半拼成双站城市，390／360改为建筑旁的紧凑文字入口。中央是连接广场，未增加退役站点。图片错误时移除图层，保留文字与按钮。`lastStation`文案为“上次到访”，未保存未完牌局。

目标工坊使用原表达式树、求解器和12题，只抽出专用`view.ts`、`styles.css`及独立`progress.ts`。牌仍按ID选取，合并后剩余牌保持顺序、新牌追加；运算先提交状态，180ms轮廓／位移动画只负责呈现。提示只在主动请求时求当前续解。换目标／换组仅在已合并的未完局出现原生取消确认；已完成局可自愿重玩／下一组。静音与减少动态沿用既有持久偏好。

新图经已授权 ChatGPT 图像能力实际生成。原图、完整prompt与来源清单在[`assets/images/math-world/`](../../assets/images/math-world/)，每个prompt保留要求的前缀。图片没有烘焙标题／数字／算式；旧图仍有Classic消费者，因此保留。

| 文件 | 尺寸／字节／alpha | SHA256 |
| --- | --- | --- |
| `two-station-city-source.png` | 1774×887；2,571,907 B；不透明 | `6baa69a9a9a257a739f7305a06e8c5181330f10f59fa56d094b96da1d87b5db2` |
| `public/assets/math-world/two-station-city.webp` | 1600×800；227,448 B；不透明 | `e73d6bebd12e30eaf4d840f1869d706335cf3f48799399a380f4991cf43d026b` |

## 已实测的行为与风险

桌面1440与触摸390各完成原12题；求解器仅在测试端选择路径，页面通过真实牌／运算／合并按钮输入。覆盖4→3→2→1、原始3与合成3、零与除零、非整除、负数与交换、提前得到目标不通关、无续解需多次撤销、完成后提示、重玩／撤销重做幂等、取消与Escape、焦点及完整历史。快速双击只消费一对；延长呈现动画的故障注入证明撤销／返回会取消呈现而不丢状态。1440／390／768／360关键按钮达到48×48 CSS px、互不相交且五个内部采样点均命中；纯键盘完成选牌、运算、交换、合并、撤销和取消。

真实200%使用任务独立Chromium profile中的`chrome.tabs.setZoom`，`getZoom=2`、DPR=2、1440浏览器窗口对应712 CSS px内容宽度。地图与目标24完整表达式／展开历史均可读，无横向溢出。此项是桌面浏览器缩放检查，未当作屏幕阅读器或真人辅助技术认证。截图归一到页首；200%用浏览器原生全页捕获，避免自动截图把缩放尺寸再除一次造成假裁切。

保存测试使用合成数据与隔离profile。37个精确键中36个可导出键通过真实Vault备份／校验／恢复逐字节往返，剩余1个恢复前备份键按既有规则生成；无关键保留。未知／未来／损坏／JSON null、拒读拒写、跨页有效换档与Vault恢复均不会被已挂载target／map覆盖。合法旧版wins及未知扩展字段保留；无新键、新schema或整局断点保存。初次Vault检查的2个失败来自测试默认取消原有确认框，接收确认后2项复验通过；未改Vault产品实现。

## 同条件资源与响应对比

`performance.json`使用5235冻结基线与5236候选生产预览直连，无代理、无节流。每个版本／尺寸／路由新建context作冷访问，再在同一context离开至空页后热返回；资源数据来自真实PerformanceResourceTiming。表内为子资源实际传输量，包含脚本／样式和图片，不把304重验证当作零网络请求。

| 路由 | 冷传输：原→新 | 热传输：原→新 | 图片RGBA解码估算：原→新 |
| --- | --- | --- | --- |
| 数学地图（1440／390相同） | 312,148→253,395 B | 1,800→1,800 B | 6,293,408→5,120,000 B |
| 目标工坊（1440／390相同） | 30,835→36,636 B | 2,100→2,400 B | 0→0 B |
| 我的游戏世界 | 6,115,311→6,120,913 B | 7,800→8,100 B | 新数学图请求0次 |

双站图只在数学地图实例化时请求，两个裁图元素共用同一资源。首页既有catalog会加载游戏定义，故新UI脚本／样式仍有约5.6KB传输增量；未改共享加载架构，首页未请求新大图。地图图片与总冷传输下降，不能据此声称整个首页变快。

每尺寸40次真实选牌输入，记录click处理开始至第二个requestAnimationFrame：桌面中位数30.6→30.6ms、P95 32.9→32.0ms；390触摸中位数32.3→32.6ms、P95 33.5→33.6ms。变化处于本机帧调度量级；这些数据不代表实体手机端到端触摸延迟。

## 最终证据、审查与发布判定

最终源树包含本文、代码、资产、测试及5张经逐图审阅后建立的数学视觉基线。首页2张和slider1张基线不更新。`final-source-freeze.json`记录该树身份、逐文件字节、原12题及受保护路径对比；`final-gates.json`逐命令记录退出码和相同源树身份；`final/`保留约31帧代表状态及输入／ARIA／几何／图片哈希。候选图已逐张审阅，最终取证仍在未改变的生产运行代码上进行。

完整门禁发现一处直接依赖回归：Portfolio Evolution的Node检查从target UI入口读取存档版本常量，新增专用CSS后触发不支持CSS扩展名的加载错误。已改为从独立`progress.ts`读取同一常量；未改检查条件、历史证据或版本值。该失败与修复后结果均保留，修复后重新冻结。

生产模式还暴露了旧退役E2E的开发地址假设：延迟加载用例只拦截`.ts`源码，已补充按既有Slider界面标记识别生产哈希chunk并延迟交付原字节。相同用例继续验证切换后旧加载不得挂载。旧用例还硬编码历史任务截图目录，已改为本次测试输出目录；其覆盖的三张历史衍生图通过冻结基线重取，SHA256与原历史保留清单完全一致后才恢复，详见`history-recovery.json`。其它门禁使用现有任务／证据目录参数隔离本轮输出。

最后一次动态反审实测到新牌的到达标记会在单独切换运算符时重播；已让标记仅供合并那次渲染消费。真实输入复验同值牌、静音／减少动态及动画取消6项通过，生产／开发延迟加载各3项通过；性能表使用此修复后的生产字节。

滚动门禁的旧假设也已按新入口结构修正：1366×768滚到底时，单个语义按钮包含的建筑顶部可在视口上方，而站名和入口提示完整可见。数学专用检查现在要求整个文字操作区完全可见、九点命中，并实际点击进入对应站点再返回；其他页面与共享滚动判定不变。不通过缩小图片或隐藏滚动来满足截图条件。

正式命令矩阵为现有affected-gates完整分支，加Pages／CI依赖的play-readiness、自然使用隐私与冒烟、英语报告一致性，以及数学无更新视觉与四尺寸几何。数学E2E以生产预览运行；其它现有产品按其原门禁配置运行。最后在冻结生产预览进行UI-only交叉自审，分别从儿童入口／操作负担、视觉与访问、异常与恢复三个角度读取同一组界面证据，不把源码可推断行为冒充UI结果。自审不是独立真人或第二位评审。

`final-verdict.json`是本地最终机器判定：只有全部要求门禁成功、保护字节未漂移、最终UI审查无阻断发现，才可为`PASS_MACHINE`。`publication.json`记录本次唯一提交的LOCAL／PUSH／Pages／CI精确SHA及工作流链接，`pages-verdict.json`记录线上构建身份和路由回读；未完成或pending不能写success。忽略证据目录不发布家庭路径／数据，正式代码和本报告随同一次提交交付。

保留的历史问题：第二章既有开发传输预算3,124,059 B大于3,043,009 B仍未关闭，本轮不抬阈值、不跳过该旧失败或写“全产品全绿”。V03的两站关联与抢焦点问题、V05的分散操作台问题由本轮实现及受影响证据处理；V01／V04／英语等其它审计项不在本轮关闭范围。

清理仅按本任务显式路径与哈希清单删除重复候选／临时profile／构建副本，保留冻结、最终证据、命令与发布回读。未制作整仓ZIP，未访问真实家庭存档。回滚时先备份既有37键，再正常revert本任务提交；不重置成绩或重写Git历史。
