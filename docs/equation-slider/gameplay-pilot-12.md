# 算式滑轨第一章 12 关玩法试点

任务 `GAME-CODEX-STEP2-SLIDER-GAMEPLAY-PILOT-12`。起点 `79109729edddb087c31bf92b15c602ebdb366808`，`main`，起始工作区干净，本地与远端一致。起点 Pages `33959837808`、Portfolio CI `33959837787` 均为实际 success。本地恢复标签：`local/slider-gameplay-pilot-12-baseline-20260905-7910972`，不推送或改写此标签。

## 改动前的设计与修复范围

问题：孩子能否从中央算式和未亮数字理解下一步，在几种成立组合间作选择，而不必重复同一套动作？机器可检查规则、内容和操作可用性，不能代替儿童回答乐趣或学习效果。

已确认的原始证据：01–05、11–12均为两轨三个一一对应数对；独立 UI 评审已实际完成 01–04，每关通关主体为一次单轨、再两次双轨切换。06–09进入三个加数且目标为11–15，10已有四条数字轨。原生成器的 `diversifyInitialIndexes` 使用全局动作计数，提前改模板会影响后续初局。`index.ts` 对任何成立关系播放 success 音，非减少动画模式进入约280毫秒 feedback-lock，即使没有新增覆盖。独立首次教程出现“上方格移入中央”与“向下移动”箭头语义误判，撤销后恢复。保存入口会把无法解析的 V3 回退为空/legacy；保存清洗器允许未知结构被后续写回覆盖。

允许修改闭包：滑轨 `levels/v3` 的定点修订集和 catalog 接线、provenance/schema、既有物化与统计工具、`index.ts`/`styles.css`/`feedback.ts`、滑轨独立保存适配与 revision 记录、直接相关逻辑/E2E、当前 README 和本报告。原 gold 模板、生成器的全局初局分配和188关定义保留；共200关，ID/顺序/章节/站点不变。保留唯一tile ID、正式reducer、同显示值拒移、动态solver、撤销/重置、全部合法解法、37键及Vault合同。没有生命、限步、最快评价、隐藏规则或强制连续12关。

下表为实现前设计。每个数列按滑轨原始循环顺序列出，初局为中央表达式。数值候选先经现有solver验证；并非把所有关都称为新策略。01完整保留，11保留棋盘并修正数学文案，02–10、12为实际棋盘修订。01–06是操作认识/配对发现；07–10含覆盖集合选择；12是组合迁移，唯一覆盖集合也不表示唯一行动顺序。

| ID | 旧问题或保留理由 | 决策 | 玩家发现 | 目标与初局；数字轨 | 一级提示（不会标格/方向） | 验收点 |
| --- | --- | --- | --- | --- | --- | --- |
| es-1-01 | 正式棋盘教程可实际完成，适合认识目标与覆盖 | 完整保留定义 | 成立一次只亮两个格，仍可找别组 | 6；4+5；[1,2,4]/[5,4,2] | 保留“6可以分成哪两个部分” | 教程真实操作、2/6后继续、重复成立不新增 |
| es-1-02 | 与01同构，UI证据显示相同通关节奏 | 修订数对和排列，作为撤掉教程的熟悉关 | 看中央数字，不照抄上一关箭头 | 5；1+3；[1,2,3]/[3,2,4] | 让中央两个数合起来是5；上方或下方的格都可以点 | 三组成立；真实点击通关，无自动高亮 |
| es-1-03 | 原仍是三个对角配对 | 修订小数值、排列与提示 | 目标命中后去找未亮格 | 6；1+3；[1,2,3]/[3,5,4] | 得到6以后，还要让没亮的数字也参加成立的算式 | 一次成立不完成；不同顺序都可覆盖 |
| es-1-04 | 原目标9的两轨配对，本身没有独立策略结构 | 修订为补10发现 | 一部分离10还差多少 | 10；4+3；[2,4,7]/[3,8,6] | 选一个数，想想它离10还差多少 | 三组互补，保持首四关至少三种首步族 |
| es-1-05 | 原数对不对称，不能直接操作交换 | 修订为对称数对 | 2+6与6+2都成立，4+4也是一组 | 8；6+6；[2,4,6]/[4,2,6] | 两个加数换个位置，总数会怎样 | 交换可见，不能把绕路说成错误 |
| es-1-06 | 原突然进入三加数到12 | 修订为两轨恒和9 | 一边多1，另一边少1仍保持和 | 9；3+5；[2,3,4]/[5,7,6] | 总数不变时，一边多1，另一边怎样变 | 2+7/3+6/4+5均可先走；未完成可撤销 |
| es-1-07 | 原三加数目标13，覆盖关系仍以给定配对为主 | 修订为两轨双目标 | 未亮数字可选不同伙伴 | 6或7，两个目标都需到过；3+5；[1,2,3]/[3,4,5] | 6和7都能点灯，找一个没亮的数字给它配伙伴 | 五个成立局面、多个覆盖集合；目标命中有可见标记 |
| es-1-08 | 原三加数加右侧5+6的等值表征 | 修订为低数值三轨支持 | 同一数字可与不同两数配合 | 6；1+2+1；[1,2,3]/[1,2,3]/[1,2,3] | 先合起两个数，再看第三个数还差多少 | 七个成立局面；三条轨都参与；同数不同格身份清楚 |
| es-1-09 | 原三加数目标15，数值负担上升 | 修订为三轨双目标 | 看未亮格选择下一组成立和 | 6或8，两个目标都需到过；3+3+4；[1,2,3]/[1,2,3]/[1,2,4] | 先看没亮的格，再试它能组成6还是8 | 十个成立局面、多个覆盖集合；乱走后三级提示可靠 |
| es-1-10 | 原四加数超出本轮温和组合上限 | 修订为三轨目标8 | 分成三部分，合起两数帮忙 | 8；1+1+2；[1,2,4]/[1,2,4]/[2,3,5] | 找一个还没亮的数字，另外两部分怎样配 | 六个成立局面；站点结尾可直接返回/休息 |
| es-1-11 | 第二站两轨目标8适合喘息，但原说明/回顾仍误写6 | 保留棋盘，修正文案6→8 | 三轨后回到熟悉的两轨补数 | 保留原物化定义与初局 | 保留原概念提示 | 第二站归属、原保存语义不变，不要求连打12关 |
| es-1-12 | 原又为两轨数值平移，重复主体关系 | 修订为三轨目标10迁移 | 用已会的补数配成三部分 | 10；1+1+3；[1,3,4]/[1,2,3]/[3,4,6] | 试着让没亮的格也参加凑10 | 五个成立局面，四组覆盖；允许所有合法次序；13关正常衔接 |

停止条件：上述闭包实现完成、必要检查通过、一次本任务提交/推送，并分别核查该SHA的Pages和Portfolio CI；不进入其余188关精修或第三步。`REAL_CHILD_FUN_AND_LEARNING=NOT_CLAIMED`。

## 实现前独立设计反审（数学 / 游戏设计）

独立源码与数学评审已读上表并用原 `publishLevel`、实时 `findHintContinuation` 及正式 reducer 逐步完成候选。结论：设计可实现，原四结构族门禁保持；01完整保留、11保留棋盘并修正文案，不能写成12关均有新玩法。01–06依然是有意保留的两数配对认识与发现，排列变化本身不构成新策略；07/09使用既有双目标，必须在可见目标中说明两个目标均需命中；08/10采用多个可覆盖组合，12的唯一最小覆盖集合不表示唯一先后。所有未完成状态可自由尝试、撤销与重置，不得用绕路或较慢方案制造错误评价。10→11保留同目标8的三部分到两部分过渡，既有站点归属不变。候选全200关数学审计通过，第一站4结构族/首四步3族、第二站4/4，重复0。

## 最终数学证据与生成隔离

单一修订源是 `games/equation-slider/levels/v3/gameplay-pilot-12.ts`，在原生成和全局初局分配结束后仅替换指定ID，再由原物化工具生成JSON。原 `gold-levels.ts`、`generator.ts` 与全部40模板保持原字节/身份；chapter 2–4文件未改。真实数量为42手工/158生成；10棋盘的revision为 `slider-pilot-12-r1`，11仅provenance文案修订 `slider-pilot-12-copy-r1`，不创建保存revision。

[机器结果](gameplay-pilot-12-math.json)可由 `pnpm exec tsx games/equation-slider/tools/check-gameplay-pilot-12.ts` 重建。全部200关可解，必需覆盖可达，发布定义校验错误0。保留188关定义去除派生analysis后逐字段规范化，基线冻结JSON和当前值的合并SHA256均为 `2c01404e63c3a627c8d7ce966b7cced980826ed769b11894741846d60d476bbe`；所有ID/顺序/章节/站点相同，01定义相同。

| 关 | 成立局面数 前→后 | 最少成立组数 前→后 | 最小覆盖集合数 前→后 | 实时solver实际动作数 前→后 |
| --- | --- | --- | --- | --- |
|01|3→3|3→3|1→1|5→5|
|02|3→3|3→3|1→1|5→5|
|03|3→3|3→3|1→1|5→5|
|04|3→3|3→3|1→1|5→5|
|05|3→3|3→3|1→1|5→5|
|06|5→3|3→3|1→1|8→5|
|07|4→5|3→4|1→2|8→5|
|08|4→7|3→3|1→2|8→9|
|09|5→10|3→4|1→2|9→8|
|10|15→6|3→3|2→2|12→10|
|11|3→3|3→3|1→1|5→5|
|12|3→5|3→4|1→1|5→10|

动作数不是最优步数或趣味评分。按忽略数值、允许轨/格重标的覆盖关系规范化，旧01–05/11/12同构，新01–06/11仍同构；新07/08/09/10/12分别有不同的关系结构。这是有意保留的熟悉阶段与后段组合选择，不能称为12种新玩法。

配对诊断对每关使用相同中央格索引起点、相同可见动作表、种子72693010和24/48次预算，分别执行固定随机序列及逐轨向下循环。轨数不同的多余轨请求消耗尝试并单独记为absent；因此不比较“通关率”作为难度。例09：随机序列旧46次/新41次首次完成；固定循环旧8次完成，新48次仅覆盖4/9。这证明该固定动作模式不再覆盖本例，不能证明好玩，也未为抗脚本增加惩罚。逐关局面、覆盖集合、solver动作和两种诊断均在JSON保留。

## 界面与保存实现

目标、剩余覆盖及多目标勾选独立显示；刚移动的中央格有轮廓，并显示“第几轨：原值→新值”。重复成立用✓与普通移动音，新进展用✦与较强反馈，全完成用★。正常成功不锁输入；拖动期间仍由原reducer/指针捕获保护。箭头改为“选上格/选下格”，点击数字、鼠标拖动、触控和键盘均复用原操作适配。一级概念提示无格/方向高亮，二级仅高亮一条轨，三级继续由当前状态solver给下一步；多步准备动作有解释。

保存仍仅写原 `family-games/equation-slider/progress-v3`。10个实际换棋盘的关在原记录内添加可选 `revisions`，旧完成、解锁、历史bestMoves/提示数保留；新版bestMoves、提示次数及开始次数独立。地图区别旧版已玩/新版在玩/新版已玩，不强制重过。01/11和其余188关无保存revision；lastLevelId、声音和教程偏好不变。当前键为损坏/未来/未知结构时只读；拒读、拒写、写回不一致或Vault/另一页面替换原始值也停止写入，当次仍可游玩。

合成保存检查覆盖旧/新完成与未完成、旧lastLevelId、撤销后保存、刷新返回、全部188条非pilot统计保持、37个精确键、退役及无关键原字节、Vault导出恢复，以及无法访问Storage。没有新增全局迁移框架、整棋盘自动保存或儿童数据。

## 独立反审、修复和验证归属

游戏设计评审使用独立模型上下文，只看可见页面、可访问控件与截图，通过正常输入完成基线及修订版12关；未读源码、设计表、隐藏DOM、存档或solver。修订01–06在独立localhost开发origin完成，遇两次HMR后，07–12及06→07重验移到冻结生产预览5178。普通桌面视口约1022×1466，10→11正常跨站；01–05未在最终冻结版重打。窄屏/后续补丁归主代理工程验证，不冒充独立盲玩或儿童试玩。

独立可见轨迹包括：08先选2+2+2，再重配未亮1/3；09最后选择目标8照顾未亮左2；12在4+2+4时仅右3未亮，提示先变为4+3+4，再选右3完成。后半段存在实际覆盖选择。评审保留两点不确定性：前六关是否偏长、10→11只有站点标题转换是否足够自然；均不构成本轮技术阻断，也不扩展成新路线。

工程/保存评审与数学/设计评审分别审查另一人的实现；本人写的数学或保存部分明确归交叉自审。共同发现并关闭：

- 教程乱走后仍指原数对：改为概念提示，撤销/任意合法数对均能恢复。
- 后续运算符格误称数字、平衡式未成立仍显示等号：修正文案与实际关系符，并在es-2-01和es-1-18复核。
- 当前键放旧格式损坏嵌套记录：完整结构检查阻止覆写；原始字节负例通过。
- 12三级提示先给预备动作却没有说明：补“这一步先准备”；桌面/窄屏/平板真实动作通关复验，390宽提示无裁切。
- 07可用1+5、2+4、3+3先亮满6格但尚缺目标7：重复反馈、目标行和一级提示转向未到过目标；实际7步复现后再选右4，合法完成。

唯一视觉基线更新为 `slider-adapter-desktop-desktop-1440-win32.png`。先逐图检查旧/新差异（目标行、动作说明、箭头文字、中央式空间），只替换该PNG，再用不更新基线模式验证。其余视觉基线未改。当前数量断言按真实42/158和同显示81/初局45/路径收益38/初局收益21更新，必须同显示移动仍为0。Portfolio Evolution校验改为读取其已绑定历史提交90eb3b2的audit，保留历史216/82及哈希断言，未重写历史证据或降低门禁。

## 本地检查结果

| 检查 | 实际结果 |
| --- | --- |
| pnpm test | 57文件、455测试通过；包含保存/进度/Vault60项与全部数学逻辑 |
| pnpm build；levels:check | 类型/生产构建通过；200关确定物化一致 |
| 滑轨现有逻辑与E2E | 基线逻辑421项、E2E42项通过；修订现有核心/发布/视觉54项及25关提示驱动工程脚本1项通过 |
| 本轮真实输入E2E | 12项通过；12关分别在1440桌面、390窄屏逐关完成，768平板09/12及es-2-11/3-11/4-01完成；教程、无新增、取消拖动、撤销、三级提示、静音/减少动画、13衔接、刷新返回与保存保护 |
| 生产portfolio smoke | 16项通过 |
| readiness + a11y + Vault浏览器 | 桌面/手机8项通过 |
| 数学世界/退役 | 48通过；9项按原配置设备条件不适用；两站入口、退役回落、20次进出、旧字节均覆盖 |
| 命中/滚动矩阵 | 命中14通过；滚动桌面2通过、手机2复验通过 |
| 保留产品 | 中文支持29通过、1失败用例单独复验通过；英语29通过/19原配置不适用；自然观察5通过/1原配置不适用 |
| 几何与单张视觉基线 | 5通过/3设备条件不适用；360/390/768/1440几何及目标工坊未改的截图通过 |
| 静态发布合同 | portfolio、evolution、readiness、interaction、scroll、privacy、chinese、english reports、math-world全部通过 |

未以skip处理失败。过程中旧数量断言、06轨数断言、多目标DOM选择器已针对真实变化更新；一次拼音点击超时和一次滚动上下文销毁发生于开发页更新阶段，冻结产品代码后仅重验受影响项通过，保留失败日志而不掩盖。PowerShell未给`@`参数加引号导致两次“无测试”属于命令错误，修正命令后运行实际矩阵。原配置中设备不适用项与失败严格分开。

最终本地冻结与原始日志保存在忽略目录 `tmp/tasks/GAME-CODEX-STEP2-SLIDER-GAMEPLAY-PILOT-12/`，其中 `final-delivery.json` 绑定最终提交/树、源哈希与远端运行结果，`baseline-comparison.json` 绑定起点物化文件。报告和代码一起提交；提交后Pages/CI精确状态写入本地交付记录及最终答复，不以自引用SHA再次修改提交。未打ZIP。

入口保持 [家庭启动器](http://127.0.0.1:5175/) 与 [数学世界滑轨](http://127.0.0.1:5175/?world=math-world&station=slider)，公开入口为 [Pages滑轨](https://archmays.github.io/Game-Codex/?world=math-world&station=slider)。回滚代码使用 `git revert <本轮最终提交SHA>` 后正常推送；本地基线标签用于只读比较，不reset或改写历史。回滚后的旧运行时不认识revision统计，若需继续旧版游玩，先用Save Vault导出当前原始记录，保留这份修订统计；不要依赖旧代码写回新格式。无需清空任何存档。

本轮判定限于技术/内容与机器操作合同。`REAL_CHILD_FUN_AND_LEARNING=NOT_CLAIMED`，真实儿童兴趣、学习效果、持续使用和首次独立理解均未验证。
