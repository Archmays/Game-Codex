# Game-Codex Natural-Use

Project mode: **NATURAL-USE ACTIVE**. The family-stable product baseline is frozen at `game-codex-family-stable-v1.0.0` (`8b890ff14880bcb576dd1ced37e14e6e3df28af1`). There is no automatic next phase and no automatic large task.

## Family quick path

Run `tools\my-game-world\START_MY_GAME_WORLD.cmd`, then use <http://127.0.0.1:5175/> in the same browser profile and origin so existing local progress remains available. The published family entry is <https://archmays.github.io/Game-Codex/>.

Use the Parent Save Vault occasionally when a local backup is useful. It exports only the declared local save keys; it is not a required routine and does not upload child data.

The Observation Kit at `?world=my-game-world&parent=observation` is optional, parent-initiated, local-only, and record-nothing by default. It has no required frequency. Open it only when a naturally occurring observation is worth preserving.

## When development resumes

Normal family use is the default. Start a new bounded change only for:

- a reproducible defect;
- concrete evidence that naturally arises during family use; or
- an explicitly chosen major expansion.

Do not infer child enjoyment, learning, retention, preference, or acceptance from machine tests. Real-child validation remains `NOT_PERFORMED_AND_NOT_CLAIMED`.
