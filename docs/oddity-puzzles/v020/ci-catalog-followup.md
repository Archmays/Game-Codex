# Pages catalog assertion follow-up

Run [35507012666](https://github.com/Archmays/Game-Codex/actions/runs/35507012666), commit `4c2820db`: portfolio smoke, first-use/return/reload, the full representative hit-test matrix and parent-only observation passed. The next command failed immediately at `tools/math-world/validate-math-world.ts:23`, whose exact Classic ID list still excluded Oddity and described four products.

The active portfolio and the existing Math World browser assertion already require five Classic cards, including Oddity. This stale validation list predates v0.2. Repair only its exact expected IDs and message; retain definition equality, uniqueness, retirement, math content, save and asset assertions. A targeted scan of the remaining release consumers found no other four-product catalog contract; numerical four-card math tasks and four-button tower controls are unrelated and remain untouched.

This extends the release-only dependency closure to one validation line, without changing any game, state, privacy, builder or workflow. The accepted product and offline ZIP bytes remain those of `2c6c17dc`; record the new validator source identity and affected checks separately in `ci-catalog-followup.json`. No CI timeout or gate is relaxed.
