# Changed-only freeze contract

## Frozen accepted identity

| Surface | Required identity |
| --- | --- |
| Encounters and first-run order | `fnv1a:d805357d` |
| Three abilities | `fnv1a:2d361817` |
| Boss phases/interference | `fnv1a:ee5df70f` |
| Theme C assets/anchors/scales | `fnv1a:15133968` |
| Final 12-character manifest | `fnv1a:67ad1fe2` |
| Review identity | `DBA281F9954DFB591E2F3D7498B1B8F09F6C050BFBFA2436C9961B0513D73D3E` |

The frozen contract includes `明、花、林、星` as the only first run; encounter order; the three accepted abilities; two boss phases and interference; Theme C keys, anchors, and scales; 3–5 minute pacing; save and safe-retry semantics; failure language; camp repairs; spellbook content; and absence from the default hub.

Audio metadata may be layered beside accepted character content only if the accepted manifest revision hash remains exact. Do not recompute or reinterpret parent acceptance after adding `visualPinyin` or `spokenPhrase`.

## Allowed changed surfaces

- explicit display/speech separation and accepted-word speech phrases;
- parent audio preflight;
- clean authorized child route chrome;
- same-origin local event bridge and observer;
- versioned observation schema, privacy validation, START/FINISH, tests, docs, Skill, evidence, and build identity.

## Automatic refusal

The STEP 04 readiness contract loads the current STEP 03 review identity and compares every frozen value to the constants above. Any difference returns:

```text
FAIL_STEP04_SCOPE_DRIFT
```

The launcher must then stop before browser launch. It may not regenerate feedback, carry acceptance onto the changed bytes, reset Git, or silently update an expected hash.

## Drift scan

Validation also checks that no new encounter, ability, boss phase, ImageGen runtime asset, default-hub entry, backend, account, remote child request, tracking field, score, leaderboard, pressure mechanic, punitive loss, or shaming failure language entered STEP 04.
